---
export interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;

const navClass = `${className || ''}`.trim();
---

<nav class={navClass} {...rest}>
  <slot />
</nav>