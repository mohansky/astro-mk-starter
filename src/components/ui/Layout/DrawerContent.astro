---
export interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;

const contentClass = `drawer-content flex flex-col ${className || ''}`.trim();
---

<div class={contentClass} {...rest}>
  <slot />
</div>