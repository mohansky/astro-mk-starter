---
export interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;

const titleClass = `footer-title ${className || ''}`.trim();
---

<span class={titleClass} {...rest}>
  <slot />
</span>